# Tallystead worker

The Docker worker currently uses `apps/api/app/worker.py` to prove the process boundary. Queue technology and handlers will be introduced with documented idempotency, retry, audit, and health requirements.
